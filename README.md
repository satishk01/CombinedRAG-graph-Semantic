"# CombinedRAG-graph-Semantic" 


#### instakk UV
curl -LsSf https://astral.sh/uv/install.sh | sh

### Create python 3.12 env and activate it
uv venv --python 3.12
source .venv/bin/activate

#### install the necessaryvc packages
pip install streamlit langchain-aws boto3

